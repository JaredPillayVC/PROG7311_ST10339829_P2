@model IEnumerable<PROG7311_ST10339829_P2.Models.Product>
@{
    // View for displaying the current farmer's products
    ViewData["Title"] = "My Products";
}

<h2>@ViewData["Title"]</h2>
<table class="table table-dark table-striped table-hover">
    <thead>
        <tr>
            <th>Name</th>
            <th>Category</th>
            <th>Production Date</th>
        </tr>
    </thead>
    <tbody>
        @* Loop through each product and display its details *@
        @foreach (var p in Model)
        {
            <tr>
                <td>@p.Name</td>
                <td>@p.Category</td>
                <td>@p.ProductionDate.ToShortDateString()</td>
            </tr>
        }
    </tbody>
</table>
<a asp-action="AddProduct" class="btn btn-success">Add New Product</a>
